/**
 * Kategori segmenti eşleşmediğinde veya parallel route fallback olarak kullanılır.
 * Next.js "No default component was found for a parallel route" uyarısını gidermek için.
 */
export default function CategoryDefault() {
  return null
}
